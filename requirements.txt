# Core dependencies for weather_updater.py
requests>=2.31.0
pandas>=2.0.0
python-dotenv>=1.0.0

# For concurrent API calls
futures3>=1.0.0

# Optional: if you need these for your specific setup
numpy>=1.24.0

# Note: The script uses these APIs (no additional packages needed):
# - Open-Meteo API (free, no key required)
# - NOAA ERDDAP for sea temperature (free, no key required)
# - Concurrent futures for parallel processing